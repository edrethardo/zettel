fastapi
uvicorn
jinja2
httpx
pytest

# Die Testsuite parallel (WB-366): `pytest -q -n auto`. Reine pip-Abhängigkeit,
# nichts davon wird zur Laufzeit des Shops importiert. Nötig ist sie nicht —
# `pytest -q` läuft weiter seriell durch, und keine Zusicherung hängt daran.
# Gemessen am 2026-08-28 auf 28 Kernen: 19,5 s seriell, 13,9 s mit `-n auto`.
pytest-xdist

openai

# Observability (Spec 7, WB-328). Geprüft am 2026-08-28 gegen ein laufendes
# Phoenix und die echte vLLM-Box mit: arize-phoenix-otel 0.17.1,
# arize-phoenix-client 3.3.0, openinference-instrumentation-openai 0.1.56,
# openai 3.5.0. Dass der Instrumentor auf openai 3.x greift, war das offene
# Risiko aus WB-326 — es ist gemessen erledigt.
arize-phoenix-otel
arize-phoenix-client
openinference-instrumentation-openai

# Nur für die Verifikationsskripte (scripts/trace_probe.py und
# scripts/label_probe.py): die Abfragen aus Spec 7.4 und 8.1 gehen über
# `spans.get_spans_dataframe()`, und das gibt einen pandas-Dataframe zurück.
# Der Shop selbst benutzt pandas nirgends.
pandas

# Miniaturen für die Produktkacheln (WB-374, picknick/miniaturen.py). Läuft im
# Nachtlauf, nicht im Antwortweg des Shops — der Web-Prozess importiert das
# Modul zwar, aber `PIL` erst innerhalb von `ableiten()`. Ein Shop ohne Pillow
# startet und liefert weiter Bilder aus, nur eben die Originale.
pillow
