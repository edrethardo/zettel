fastapi
uvicorn
jinja2
httpx
pytest
openai

# Observability (Spec 7, WB-328). Geprüft am 2026-08-28 gegen ein laufendes
# Phoenix und die echte vLLM-Box mit: arize-phoenix-otel 0.17.1,
# arize-phoenix-client 3.3.0, openinference-instrumentation-openai 0.1.56,
# openai 3.5.0. Dass der Instrumentor auf openai 3.x greift, war das offene
# Risiko aus WB-326 — es ist gemessen erledigt.
arize-phoenix-otel
arize-phoenix-client
openinference-instrumentation-openai

# Nur für die Verifikationsskripte (scripts/trace_probe.py und
# scripts/label_probe.py): die Abfragen aus Spec 7.4 und 8.1 gehen über
# `spans.get_spans_dataframe()`, und das gibt einen pandas-Dataframe zurück.
# Der Shop selbst benutzt pandas nirgends.
pandas
